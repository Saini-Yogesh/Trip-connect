# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Provide your MongoDB Atlas connection string here
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/tripconnect

# Authentication Configuration
# Provide a strong random secret key for JWT tokens signing
JWT_SECRET=your_jwt_signing_secret_key
